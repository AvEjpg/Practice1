# README

## 📂 Структура проекта

```
Practice1/
├─ app/                    
│  ├─ frontend/            # Фронтенд на Flask
│  │  ├─ app.py            # Основной файл Flask, маршруты и вызовы API
│  │  ├─ templates/        # Jinja2-шаблоны
│  │  │  ├─ base.html              # Общий каркас (хедер, сайдбар, футер)
│  │  │  ├─ products.html          # Таблица продукции
│  │  │  ├─ product_form.html      # Форма добавления/редактирования продукции
│  │  │  ├─ workshops.html         # Общий список цехов
│  │  │  ├─ product_workshops.html # Цеха конкретного продукта
│  │  │  └─ calculation.html       # Калькулятор сырья
│  │  └─ static/           # Статические файлы (логотип, иконка, стили)
│  │     ├─ logo.png
│  │     └─ logo.ico
│  │
│  ├─ repositories/        # SQL-запросы к таблицам
│  │  ├─ products_repo.py
│  │  ├─ product_types_repo.py
│  │  ├─ material_types_repo.py
│  │  └─ workshops_repo.py
│  │
│  ├─ schemas/             # Pydantic-схемы
│  │  ├─ product.py
│  │  ├─ product_type.py
│  │  ├─ material_type.py
│  │  ├─ workshop.py
│  │  └─ calc.py
│  │
│  ├─ services/            # Бизнес-логика
│  │  ├─ product_service.py
│  │  ├─ product_types_service.py
│  │  ├─ material_types_service.py
│  │  ├─ workshops_service.py
│  │  └─ calc_service.py
│  │
│  ├─ .env                 # Конфигурация подключения к БД
│  ├─ config.py            # Загрузка настроек из .env
│  ├─ db.py                # Подключение к PostgreSQL, execute_query()
│  └─ main.py              # FastAPI-эндпоинты
│
├─ отчеты/                 # Документы по заданиям и диаграмма
│  ├─ отчет1.docx          # Отчёт по первому заданию
│  ├─ отчет2.docx          # Отчёт по второму заданию
│  ├─ отчет3.docx          # Отчёт по третьему заданию
│  ├─ отчет4.docx          # Отчёт по четвёртому заданию
│  └─ ERpgerd.pdf          # ER-диаграмма базы данных
│
└─ requirements.txt        # Зависимости проекта

```

## ⚙️ Установка

### 1. Клонирование репозитория

```bash
git clone <URL вашего репозитория>
cd project_root
```

### 2. Python окружение и зависимости

```bash
python -m venv .venv
# Windows PowerShell:
. .venv/Scripts/Activate.ps1
# Linux/macOS:
source .venv/bin/activate

pip install -r requirements.txt
```


## 🗄️ Конфигурация

### .env

В папке `app/` измените файл `.env`:

```env
DATABASE_URL=postgresql+psycopg://ИМЯ_ПОЛЬЗОВАТЕЛЯ(стандартное: postgres):ПАРОЛЬ@localhost:5432/НАЗВАНИЕ_БАЗЫ_ДАННЫХ
```

### База данных

Создайте БД (не забудьте название вписать в .env) в PostgreSQL.

Импортируйте таблицы через `pgAdmin4`.

Ожидаемые таблицы:

*   `Products_import`
*   `Product_workshops_import`
*   `Workshops_import`
*   `Product_type_import`
*   `Material_type_import`

## 🚀 Запуск

### 1. Бэкенд (FastAPI)

```bash
cd app
uvicorn main:app --reload
```

Приложение доступно на `http://127.0.0.1:8000`

Документация: `http://127.0.0.1:8000/docs`

### 2. Фронтенд (Flask)

```bash
cd app/frontend
python app.py
```

Интерфейс доступен на `http://127.0.0.1:5000`

Навигация:

*   📦 Продукция
*   🏭 Цеха
*   📐 Расчёт сырья
*   ➕ Добавить продукт

## 🔗 Эндпоинты FastAPI

*   `GET /products` — список продукции с временем производства
*   `POST /products` — добавление
*   `PUT /products/{id}` — редактирование
*   `DELETE /products/{id}` — удаление
*   `GET /workshops` — общий список цехов
*   `GET /products/{id}/workshops` — цеха конкретного продукта
*   `GET /product_types` — типы продукции (коэффициенты)
*   `GET /material_types` — типы материалов (процент потерь)
*   `GET /calculate_raw_material` — расчёт сырья


## 🎨 Стиль

*   **Шрифт**: Candara
*   **Цвета**:
    *   Основной фон: `#FFFFFF`
    *   Дополнительный фон: `#D2DFFF`
    *   Акцент: `#355CBD`
*   Логотип в хедере, иконка приложения (`logo.ico`).

## 🛠️ Типичные проблемы


*   **Импорт ошибок** (`product_service` vs `products_service`) → проверьте совпадение имён файлов и модулей.
*   **Нет данных в таблицах** → проверьте импорт CSV/SQL и корректность имён колонок.
